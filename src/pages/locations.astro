---
import Layout from '../layouts/Layout.astro';
import LocationsComponent from '../components/Locations.jsx';
import Wrapper from '../components/Wrapper.jsx';
---

<Layout>
  <Wrapper client:only="react" currentPath={Astro.url.pathname}>
    <LocationsComponent client:only="react" />
  </Wrapper>
</Layout>
